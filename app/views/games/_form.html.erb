<%= form_for(@game) do |f| %>
<% if @game.errors.any? %>
<div id="error_explanation">
  <h2><%= pluralize(@game.errors.count, "error") %> prohibited this user from being saved:</h2>

  <ul>
    <% @game.errors.full_messages.each do |msg| %>
    <li><%= msg %></li>
    <% end %>
  </ul>
</div>
<% end %>

<%= f.label :player2_id %>
<%= f.collection_select( :player2_id , User.all.collect, :id, :username, :prompt => true) %>


<div class="actions">
  <%= f.submit %>
</div>
<% end %>